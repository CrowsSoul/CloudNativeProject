apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-client
  namespace: stockmgr
spec:
  replicas: 1
  selector:
    matchLabels:
      project: stockmgr
      app: employee-client
  template:
    metadata:
      labels:
        project: stockmgr
        app: employee-client
    spec:
      imagePullSecrets:
      - name: registry-pull-secret
      containers:
      - name: employee-client
        image: registry.cn-hangzhou.aliyuncs.com/my-stock/employee-client:latest
        imagePullPolicy: IfNotPresent
        ports:
          - protocol: TCP
            containerPort: 8018
        env:
          - name: JAVA_OPTS
            value: "-Xmx1g"
        resources:
          requests:
            cpu: 0.5
            memory: 256Mi
          limits:
            cpu: 1
            memory: 1Gi
        readinessProbe:
          tcpSocket:
            port: 8018
          initialDelaySeconds: 60
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 8018
          initialDelaySeconds: 60
          periodSeconds: 10
